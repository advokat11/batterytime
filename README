
## Описание

Данный код представляет собой простую программу на Go, которая создаёт файл `times.txt` на рабочем столе пользователя и записывает в него различные временные метки.

## Функциональность

1. Программа создаёт файл `times.txt` на рабочем столе пользователя.
2. Записывает текущее время в первую строку файла.
3. Ждёт 30 минут и записывает текущее время во вторую строку.
4. Ждёт ещё 30 минут и записывает текущее время в третью строку.
5. После этого программа ожидает, пока заряд ноутбука достигнет 100%, и записывает время этого события в четвёртую строку.

## Зависимости

Программа использует сторонний пакет `github.com/distatus/battery` для определения уровня заряда батареи ноутбука.

## Инструкция по использованию

1. Убедитесь, что у вас установлен [Go](https://golang.org/).
2. Установите необходимый пакет:

```
go get github.com/distatus/battery
```

3. Склонируйте репозиторий или скачайте и распакуйте исходный код.
4. Перейдите в каталог с исходным кодом и выполните:

```
go run <имя_файла>.go
```

Где `<имя_файла>.go` - это имя вашего файла с исходным кодом.

После выполнения программы на рабочем столе пользователя появится файл `times.txt` с временными метками.
